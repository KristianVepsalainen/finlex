% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_text.R
\name{flx_get_text}
\alias{flx_get_text}
\title{Get the full text of one or more Finnish statutes, by section}
\usage{
flx_get_text(year, number)
}
\arguments{
\item{year}{Integer vector. Year(s) of the statute(s).}

\item{number}{Integer vector. Number(s) of the statute(s) within that
year. Recycled against \code{year} if shorter.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per section (or one row with
\code{NA} section fields if the statute has no sections, was not found, or
an error occurred) and the columns:
\describe{
\item{year}{Year of the statute, as supplied.}
\item{number}{Number of the statute, as supplied.}
\item{status}{One of \code{"ok"}, \code{"no_sections"} (statute found, but
contains no \code{section} elements — for example a very short
decision), \code{"not_found"}, or \code{"error"}.}
\item{section_index}{Integer. The section's position within the
statute (1-based), in document order.}
\item{section_id}{Character. The section's Akoma Ntoso \code{eId}
attribute, if present (for example \code{"sec_1"}).}
\item{heading}{Character. The section heading, if present.}
\item{text}{Character. The full text of the section, including its
heading.}
}
}
\description{
Retrieves the body text of a Finnish statute from the Finlex Open Data
API, split into one row per section (\emph{pykälä}). This complements
\code{\link[=flx_get_metadata]{flx_get_metadata()}}, which only reports the \emph{number} of sections; this
function returns their actual content.
}
\details{
Statutes can be long. Calling this function for many statutes at once
will be slow and will retrieve a large amount of text — consider
narrowing down to the statutes you actually need (for example with
\code{\link[=flx_download_statutes]{flx_download_statutes()}} or \code{\link[=flx_get_metadata]{flx_get_metadata()}}) before calling this
function.
}
\examples{
\donttest{
flx_get_text(year = 1992, number = 1535) # Tuloverolaki, all sections
}
}
