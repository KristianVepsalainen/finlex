% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_title.R
\name{flx_get_title}
\alias{flx_get_title}
\title{Get the title of one or more Finnish statutes}
\usage{
flx_get_title(year, number)
}
\arguments{
\item{year}{Integer vector. Year(s) of the statute(s).}

\item{number}{Integer vector. Number(s) of the statute(s) within that
year. Recycled against \code{year} if shorter.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per \verb{(year, number)} pair and
the columns:
\describe{
\item{year}{As supplied.}
\item{number}{As supplied.}
\item{status}{One of \code{"ok"}, \code{"not_found"}, or \code{"error"}.}
\item{title}{The statute's official title, or \code{NA} if unavailable.}
}
}
\description{
Retrieves the official title of one or more Finnish statutes from the
Finlex Open Data API, given each statute's year and number.
}
\details{
Not every statute in Finnish legal history is available through the
Finlex Open Data API (very old statutes in particular are not covered).
When a statute cannot be found, \code{status} is set to \code{"not_found"} rather
than raising an error, so that vectorized calls complete even when some
statutes are missing.
}
\examples{
\donttest{
flx_get_title(year = 1992, number = 1535) # Tuloverolaki
flx_get_title(year = c(1992, 1993), number = c(1535, 1501))
}
}
