% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_metadata.R
\name{flx_get_metadata}
\alias{flx_get_metadata}
\title{Get structured metadata for one or more Finnish statutes}
\usage{
flx_get_metadata(year, number)
}
\arguments{
\item{year}{Integer vector. Year(s) of the statute(s).}

\item{number}{Integer vector. Number(s) of the statute(s) within that
year. Recycled against \code{year} if shorter.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per \verb{(year, number)} pair and
the columns:
\describe{
\item{year}{As supplied.}
\item{number}{As supplied.}
\item{status}{One of \code{"ok"}, \code{"not_found"}, or \code{"error"}.}
\item{date_issued}{Date the statute was issued, as a \link{Date}.}
\item{title}{The statute's official title.}
\item{n_sections}{Integer. Number of \code{section} elements found in the
Akoma Ntoso document — a rough, purely structural proxy for
statute length.}
}
}
\description{
Retrieves core bibliographic metadata for one or more Finnish statutes
from the Finlex Open Data API: the date of issue, the official title,
and the number of sections (\emph{pykalat}) in the statute text.
}
\details{
This function intentionally returns only raw, directly observed fields.
Any derived classification (for example, grouping statutes into
categories such as "Act", "Decree", or "Decision" based on title
patterns) is left to a separate function, since that involves
methodological choices rather than data retrieval.
}
\examples{
\donttest{
flx_get_metadata(year = 1992, number = 1535) # Tuloverolaki
}
}
