% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_consolidated_text.R
\name{flx_get_consolidated_text}
\alias{flx_get_consolidated_text}
\title{Get the consolidated (up-to-date) text of one or more Finnish statutes}
\usage{
flx_get_consolidated_text(year, number)
}
\arguments{
\item{year}{Integer vector. Year(s) of the statute(s) as originally
enacted. The consolidated version shares the same year/number as the
original statute -- it is a different rendering of the same legal
work, not a separate document.}

\item{number}{Integer vector. Number(s) of the statute(s) within that
year. Recycled against \code{year} if shorter.}
}
\value{
A tibble with one row per section (or one row with \code{NA}
section fields if the statute has no sections, has no consolidated
version, was not found, or an error occurred) and the columns:
\code{year}, \code{number}, \code{status} (one of \code{"ok"}, \code{"no_sections"},
\code{"not_found"}, or \code{"error"}), \code{section_index}, \code{section_id},
\code{heading}, and \code{text}.
}
\description{
Retrieves the \emph{consolidated} version of a statute -- the text as
currently in force, with all amendments merged in -- split into one
row per section (Finnish: \emph{pykala}). This complements \code{\link[=flx_get_text]{flx_get_text()}},
which returns the statute's \emph{original}, as-enacted text.
}
\details{
Not every statute has a consolidated version available -- for example,
very recent statutes that have never been amended, or statutes outside
the API's consolidation coverage, will return \code{status = "not_found"}.
}
\examples{
\donttest{
flx_get_consolidated_text(year = 1992, number = 1535) # Tuloverolaki
}
}
