% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_affected.R
\name{flx_get_affected}
\alias{flx_get_affected}
\title{Get statutes affected by an amending or repealing statute}
\usage{
flx_get_affected(year, number)
}
\arguments{
\item{year}{Integer vector. Year(s) of the amending/repealing statute(s).}

\item{number}{Integer vector. Number(s) of the statute(s) within that
year. Recycled against \code{year} if shorter.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the columns:
\describe{
\item{source_year}{Year of the amending/repealing statute, as
supplied.}
\item{source_number}{Number of the amending/repealing statute, as
supplied.}
\item{status}{One of \code{"ok"} (at least one affected statute found),
\code{"no_affected"} (statute found, but no affected-document
references present), \code{"not_found"}, or \code{"error"}.}
\item{target_href}{URI of the affected statute, or \code{NA}.}
\item{target_year}{Year of the affected statute, or \code{NA}.}
\item{target_number}{Number of the affected statute, or \code{NA}.}
}
}
\description{
For a given amending or repealing statute, retrieves the statute(s) it
affects (the \code{affectedDocument} references in the Akoma Ntoso
document). A single amending statute can affect more than one target
statute (for example, an omnibus act amending several other acts), in
which case one row is returned per affected target.
}
\details{
This function does not check whether the supplied statute is actually
an amendment or repeal; it simply looks for \code{affectedDocument}
references in whatever statute is requested. New statutes will
typically return \code{status = "no_affected"}.
}
\examples{
\donttest{
# Look up affected statutes for a known amending statute
flx_download_statutes(
  start_year = 2023, end_year = 2023,
  categories = "amending-statute"
) |>
  head(1) |>
  with(flx_get_affected(year, number))
}
}
