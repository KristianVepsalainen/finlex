% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_doc.R
\name{flx_get_doc}
\alias{flx_get_doc}
\title{Get a Finlex document by type, year, and number}
\usage{
flx_get_doc(doc_type, year, number)
}
\arguments{
\item{doc_type}{Character, length 1. One of \code{"authority-regulation"},
\code{"treaty"}, \code{"tax-treaty-consolidated"}, \code{"government-proposal"},
\code{"collective-agreement-general-applicability-decision"}, or
\code{"trade-union-center-agreement"}. Call this function separately for
different document types.}

\item{year}{Integer vector. Year(s) of the document(s).}

\item{number}{Character or integer vector. Number(s) of the
document(s) within that year. Recycled against \code{year} if shorter.
Accepts character because document numbers in these collections are
not always simple integers.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per document and the
columns:
\describe{
\item{doc_type}{As supplied.}
\item{year}{As supplied.}
\item{number}{As supplied.}
\item{status}{One of \code{"ok"}, \code{"pdf_only"} (the document exists but
has no machine-readable text, only a scanned/original PDF),
\code{"not_found"}, or \code{"error"}.}
\item{title}{The document's official title.}
\item{date_issued}{Date the document was issued, as a \link{Date}.}
\item{text}{The full body text of the document, or \code{NA} if only a
PDF is available.}
\item{pdf_url}{URL of the document's PDF version. Populated for
both \code{"ok"} and \code{"pdf_only"} statuses.}
}
}
\description{
Retrieves a document from the generic document endpoint of the Finlex
Open Data API (\verb{/akn/fi/doc/...}), which covers document types that are
not part of the Statute Book of Finland: treaties, government
proposals, authority regulations, and certain collective agreements.
}
\details{
Unlike \code{\link[=flx_get_text]{flx_get_text()}}, this function does not split the document
into sections. The document types covered here (treaties, government
proposals, agreements) do not share a consistent internal structure
the way statutes do, so the full body text is returned as a single
block.

Some documents, particularly older ones, have never been digitised as
machine-readable text and exist only as a scanned or original PDF. For
these, \code{status} is \code{"pdf_only"}, \code{text} is \code{NA}, and \code{pdf_url} points
to the PDF so the content is still reachable.

Two document types available in the underlying API are intentionally
\emph{not} covered by this function: \code{"treaty-metadata"} (a lighter
reference record rather than a full document) and
\code{"legal-literature-references"} (an index of literature discussing
case law, not the case law itself). Both likely need bespoke handling
and may be added in a future version.
}
\examples{
\donttest{
flx_get_doc(
  doc_type = "authority-regulation",
  year = 1996, number = 32082
)
}
}
